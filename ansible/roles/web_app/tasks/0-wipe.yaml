---
- name: Remove container
  community.docker.docker_container:
    name: "{{ web_app_container_name }}"
    state: absent
  tags: [wipe]

- name: Remove docker-compose file
  ansible.builtin.file:
    path: /web-app/docker-compose.yaml
    state: absent
  tags: [wipe]